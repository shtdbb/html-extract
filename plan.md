# 执行蓝图（plan）

> 课题：「构建离线库的第一步——解析原始 HTML，提取 title、content、author、time 等结构化字段」
> 预算：约两天；目标：**可验证的原型 + 真实评测数据 + 决策记录**，不追求通用工业级系统。

---

## §1 任务边界

**做什么**：输入是爬虫抓取的**原始 HTML 文件**（本地静态解析，不做 JS 渲染、登录、反爬对抗），输出**结构化记录**（至少 title / content / authors / time），双形态（机器可读 JSONL + 人类可读 Markdown）。核心难点是从 `<title>` 剥站点后缀拿到真标题、从 `<body>` 中剥离 boilerplate（导航/侧栏/页脚/相关推荐）只留正文。

**不做什么**：
- 不做爬虫/抓取本身，不做 JS 渲染（headless browser）——渲染只作为**标记回补**（`needs_render`）留待后续管线；
- 不做存储与检索（库选型、索引进库是后续工序）——但字段设计需考虑下游建索引依赖；
- NLP 深加工（摘要、实体识别、去重归一）**可以做**但不进 baseline 主线；
- 模型生成/选元素、浏览器渲染兜底**不进 baseline**，由证据门控触发（渲染兜底：`empty_shell`/`needs_render` 占比 >5%；模型兜底：迭代后字段 miss 率仍 >30% 且无 JSON-LD 可走，且模型输出必须可映射回原文节点否则不采用）。

**三条关键风险**：① 站点形态（固定站 vs 未知模板）决定技术路线，选错路线会耗尽预算；② 字段语义与容差必须先冻结再跑分，否则分数无意义；③ 失败可观测性——漏抽（可回补）与错抽（污染库）代价不同，评测必须分字段、分错误类型。

---

## §2 默认假设（五条，被纠正前按此推进）

| # | 假设 | 内容 |
|---|---|---|
| 1 | **输入** | 本地已有一批静态 HTML（新闻/资讯/博客类中文为主、混少量英文）；需 JS 渲染出正文的页面单独标记为 out-of-scope（`needs_render`） |
| 2 | **规模** | 约百页量级人工标注 gold + 可选更大未标注池；按模板整体划分开发集/预留测试集，不按同模板随机拆分 |
| 3 | **字段** | 以《字段语义规范 v1.2》为准：time 多字段分别维护、`XX` 占位表达精度、带 `utc_offset`；content 双层（无损 content_text + 增强 content_md）；authors 为列表；images 独立字段+正文占位符 |
| 4 | **技术路线** | **固定站规则库 + 通用提取兜底**的双层架构：固定站点沉淀 CSS/XPath 规则库保稳定准确，未命中站点走通用提取（JSON-LD/meta 优先 → 通用库 → DOM 兜底）保覆盖；工具版本与默认参数以官方文档为准 |
| 5 | **评测** | 评分规则**先冻结再跑分**；逐页评分宏平均、分字段分错误类型统计、失败不从分母消失；任何修订登记决策日志并重跑全部对照；不调预留测试集 |

---

## §3 八阶段

| 阶段 | 做什么 | 关键产出 |
|---|---|---|
| ① 理解任务 | 复述边界、摆未知条件与风险、立默认假设 | 本文档 |
| ② 定义字段 | 敲定字段语义与格式，对齐行业标准（schema.org/NewsArticle + Open Graph、WHATWG Encoding、ISO 8601-2 EDTF、RFC 3339） | `field_spec_v1.md`（v1.2 冻结） |
| ③ 准备数据 | 采集/整理 HTML 样本，保存原始 HTML + URL + 采集时间 + 编码 + 内容哈希；按站点与模板分组，模板级切分开发集/预留测试集；固定站保稳定准确、通用池保多样覆盖 | 样本清单 + 数据集索引 |
| ④ 建立评测 | 写标注指南并标注 gold（gold 边界 = HTML 中存在的信息，禁提取器输出直接入 gold、禁联网补齐）；冻结评分脚本 | `annotation_guide.md`、gold 集、`eval_spec_v2.md` + `score_v2.py`（绑定冻结） |
| ⑤ 跑 baseline | 先查官方文档核实工具版本/参数，再真实安装运行；JSON-LD/meta 优先 → 站点规则库 → 通用库兜底，外加纯正文经典算法作对照臂 | 实测报告（真实日志、真实分数） |
| ⑥ badcase 分析 | 分字段、分错误类型（漏提/误填/类型混淆/薄页召回失败/后缀未剥等）统计归因；输入质量与提取质量分开 | 失败分类表 + 归因 |
| ⑦ 有限迭代 | 每轮只改一类问题，改前改后各跑一次全量评测；回归即回滚留痕；不超 2~3 轮 | 迭代记录 + 各版本分数 |
| ⑧ 整理交付 | 代码 + 评测报告 + 决策日志 + 使用说明；测试集仅在交付时跑一次、只看聚合分 | 交付物落盘 |

**全程纪律**：区分事实（官方文档）/ 假设 / 实测结果，不编造日志与分数；写代码后实际运行，未运行就明说；不通过修改标准答案或评分规则制造提升；预留测试集与调优隔离，做不到完全隔离的部分如实声明。
