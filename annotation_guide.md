# 标注指南（gold 标注操作手册）

- **读者**：标注者（人或子 agent）。
- **依据**：字段语义以《字段语义规范 v1.2（冻结版）》为唯一标准；本指南只讲"怎么标、去哪找证据、怎么防错"，不重新定义字段。
- **铁律**：gold 以输入 HTML 中存在的信息为边界；**不得直接采用任何提取器的输出作为 gold**，不得联网补齐。

---

## §1 工作流程总览

1. 打开原始 HTML（按 `source.encoding.used` 解码后的形态），同时在浏览器中渲染对照——但**标注只以 HTML 源码中存在的信息为准**。
2. 先判页面类型（`content_structure` + `page_condition`），决定是否属于"应拒识/低置信"样本（§6）。
3. 文章页逐字段标注（§2），每个字段填写 `provenance`（来源证据）。
4. 标完过自动化结构核查（§7），存疑项登记争议表（§8）。
5. 标注工具（如 trafilatura 草稿）**只允许用于起草初稿**，起草稿必须逐字段人工核对后才能成为 gold。

---

## §2 逐字段标注方法

### 2.1 title

- **证据位置**（按优先级）：JSON-LD `headline` → `og:title` / `<meta name="title">` → `<h1>` → `<title>` 标签。
- **操作**：取真实文章标题，**剥掉站点后缀与栏目名**（`_财经频道_新浪网`、`| 运动周刊`、`- 城市晚报` 等形态）。
- **交叉验证**：至少两处证据一致才可定稿；冲突时在争议表登记。

### 2.2 authors

- **证据位置**：JSON-LD `author`（注意可为数组、可为 Organization）、`meta[name=author]`、byline 区块（`作者：`、`记者：`、`By ` 等）、正文尾部署名行。
- **操作**：
  - 列表逐个录入，保留原文写法；摄影者入列并标注角色：`张三（摄影）`；
  - **责任编辑类推入 authors**（同摄影规则），但责编行**允许同时留在正文**中，不强制剔除；
  - 多作者（`记者 A ｜ 通讯员 B`、JSON-LD author 数组）全部入列。
- **禁止**：把站点名 / 栏目名 / `来源：XX 报`（那是 publisher）填入 authors。

### 2.3 时间字段组

- **证据位置**：`<time datetime="...">` 属性（**优先于显示文本**）、JSON-LD `datePublished`/`dateModified`、`article:published_time` / `article:modified_time` meta、byline 行内日期。
- **操作**：
  - 发布、更新**分别标注**，逐字段填 `publish_time` / `update_time`，严禁混淆；
  - 值统一为 `YYYY-MM-DD hh:mm:ss`，**未知的分量用 `XX` 占位**（只知到日就 `2024-03-21 XX:XX:XX`，只知年月未知年就 `XXXX-09-XX ...`）；
  - 显示文本有歧义时（`09/05/2026` 是 9 月 5 日还是 5 月 9 日？）以 `datetime` 属性 / JSON-LD 等机器可读证据消歧；无消歧证据时，可疑分量按未知处理（`XX`），不猜；
  - `utc_offset`：页面显式给出才记；按站点区域推断的值必须标 `utc_offset_source: inferred_site_locale`；未知记 `null`，**不强转 UTC**；
  - 合理性自检：更新晚于发布为正常方向，反向要复核是否把两个时间标反了。

### 2.4 content_text / content_md / images

- **正文容器定位**：先找正文容器（`article`、站点正文 id/class、文本密度高且链接密度低的子树），只取容器内文本。
- **操作**：
  - 保留段落换行，连续多个 `\n` 合并为 1~2 个；
  - 图片位置插入规约占位符（`[[IMG_1]]`…），并在 `images` 列表录入类型/说明文字/url/尺寸（HTML 中可知才记）；
  - 表格/列表/公式块/代码块：content_text 保留纯文本，content_md 转 GFM；转换有损时在 `conversion_flags` 登记；
  - 正文容器内部的杂质（责编行除外，按规则可留）——版权声明、"转载请注明"、相关推荐、分享按钮文字——一律剔除。
- **禁止**：把导航、页脚、侧栏、"相关阅读"混入正文。

---

## §3 常见误判清单（高发错误，标注前必读）

| 误判 | 正确做法 |
|---|---|
| `<title>` 带站点后缀直接当标题 | 剥后缀，取真标题（§2.1） |
| 把「编辑：李四」当作者 | 编辑类推入 authors 但**不与报道作者混为一人**；byline 整段不得直接抓 |
| 把「来源：城市晚报」当作者 | 那是 publisher（机构/转载出处），永不进 authors |
| 多日期混淆：全文正则抓日期 | 页面常含发布、更新、正文事件日期（如"自9月1日起"）、页脚版权年等多个日期样字符串——按语义字段分别认，优先 `datetime` 属性 |
| 正文容器内的责编行被当噪声删掉 | 责编可留正文（同时入 authors），不强制剔除 |
| h1 盲信 | 存在 h1 是博主名 / 频道名的站点，h1 只是候选证据之一 |
| 把图集的图片说明序列当普通段落正文 | 先判 `content_structure=photo_set`，按图集规则标 |
| 把 `<script>` 内嵌 JSON 里的数据当正文/图片 | 从严：script 内嵌数据**暂不纳入** gold |

---

## §4 防循环论证条款（必须遵守）

1. gold 必须**独立于提取器核对**：允许提取器起草，但每个字段必须由标注者在 HTML 原文中找到独立证据后方可确认。
2. 标注者与提取器实现者对同一争议各执一词时，以 HTML 原文证据为裁判，登记争议表（§8）。
3. 禁止"提取器说是就是"式确认；禁止为了让 baseline 分数好看而回改 gold——gold 修订只能因为"发现了标注错误"，且必须走 changelog。
4. 复核人抽查时，必须能从 `provenance` 独立复现该字段的取值过程。

---

## §5 拒识样本怎么标

以下样本标为"应拒识/低置信"，**不标四字段**（或标为全空），转入拒识评测单元：

- `content_structure = list_page`：频道索引、榜单页、文档索引、FAQ 列表、数据库落地页——**按页面功能分类，不按文本量**（列表页也可能有大段正文样文本）；
- `photo_set`：正文=图片说明序列的图集页；
- **付费墙页**：因付费被阻挡、HTML 中只有部分内容且无法确保有效性的，按"无可抽取"处理，content 标空；
- **视频页**：HTML 中无文本正文的，content 标空（评测规则：提取器返回空=正确，返回导航=错误）；
- `page_condition ∈ {empty_shell, anti_bot_suspected, truncated}`：标记并置 `needs_render=true`，留待渲染管线回补，不强行标注。

---

## §6 质量复核流程

1. **自动化结构核查（100% 覆盖）**：schema 校验（字段类型、时间格式与 XX 占位合法性、images 占位符与列表下标对应、provenance 齐全）、时间分量逻辑（update ≥ publish）、占位符/锚点一致性。
2. **针对性全量复查**：凡自动化发现过一类问题（如某站署名形态导致漏抓），该类问题在**全部样本**上复查一遍，不只修暴露的几页。
3. **人工抽查**：每个站点/模板至少抽 1 篇完整通读；整体抽查比例不低于 **10%**，且必须覆盖每个 `content_structure` 类别。
4. **验证深度如实声明**：复核做到了哪一层（结构核查 / 字段级 / 逐字通读）必须写进交付说明，未逐字通读就明说未逐字通读。
5. 时间字段的值需 100% 有出处（provenance 可回溯）。

---

## §7 争议登记格式

任何拿捏不准的取舍，登记到争议表后**挂起该样本**，等负责人裁决后再定稿。格式：

```
| # | 样本ID | 争议描述 | 我的处理（倾向） | 需要确认的问题 | 裁决结果 | 决策日志编号 |
```

示例：

```
| 3 | sina_album_01 | 图集图片 URL 懒加载在 <script> 内嵌 JSON 里 | 只收 HTML 文本中的图说，未解析 script | script 内嵌 JSON 算不算"HTML 中存在的信息"？ | 暂不纳入，从严 | D8 |
```

裁决生效后，争议表对应行回填裁决结果与决策日志编号；同类情形按裁决类推执行，不重复登记。
