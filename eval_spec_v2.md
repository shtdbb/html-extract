# 评测规范 v2（冻结版）

- **文档性质**：本文件是评分器 `score_v2.py` 的唯一实现依据。二者绑定冻结（见 §9 冻结条款）。
- **适用范围**：离线库 HTML 结构化提取课题的全部对照实验评分，覆盖自建数据集（开发集 / 预留测试集）与外部适配数据集（如 ISPRAS 英文子集）。
- **版本**：v2（冻结）。自冻结之日起，只与自身迭代版本纵向比较，不与外部论文口径横向比较。
- **验算**：§8 手算例子的全部断言已由 `eval/handcheck_draft.py` 实际运行验证（13 场景 / 46 断言，全部通过）。

---

## §1 总则

1. **逐页评分，宏平均**：每个页面独立评分，字段分数为各页分数的算术平均——每页等权，不按文本长度加权。
2. **分组报告**：除总宏平均外，必须按以下维度分组输出：
   - 数据集（自建 dev / 自建 test / 外部数据集各自独立）；
   - 站点 / 路由层（固定站点·规则路由、通用站点·兜底路由）；
   - 页面类型（`content_structure`：single / multi_article / photo_set / live_blog / list_page 等）。
3. **失败不从分母消失**：提取器崩溃、漏提（返回空/null）、误填（gold 为空却填值）**全部计 0 进入分母**。不存在"这页跳过"的默认行为。
4. **唯一例外——双端皆空**：gold 某字段为空 **且** pred 同字段也为空，视为"正确留空"（correct_empty）。该页该字段**不进入分母**，单独累计 `correct_empty` 计数并上报。
5. **非文章页不进字段评分**：`content_structure` 为非文章类型（list_page 等）及 `page_condition` 非 ok 的页面，不参与四字段评分，只参与拒识准确率（§7）。
6. **空值语义**：gold 空 = 输入 HTML 中不存在该字段信息；pred 空 = 提取器未产出。两者都不得被静默改写成非空值。
7. **中文处理**：所有文本类指标一律**字符级**计算，不分词、不调用任何 tokenizer——中文免分词、英文免 tokenizer 差异，同一规则覆盖中英。

---

## §2 通用规范化

以下规范化函数是全文档共用的，实现时必须先实现并单测：

```
norm_text(s) = 删除全部空白字符( casefold( NFKC(s) ) )
```

- NFKC：统一全半角、兼容字符（如全角数字４２→42，全角空格→普通空格后被删）。
- casefold：大小写折叠（比 lower 更彻底，德文 ß 等也折叠）。
- 删除全部空白：空格、制表符、换行（含 `\n`、`\r\n`）、不间断空格（NFKC 后转为普通空格）一律删除。段落结构信息不进主指标，由 `para_ratio` 诊断单独考察。

---

## §3 title

| 项 | 规定 |
|---|---|
| 分子 / 分母 | 每页 0/1；分母 = 全部参评页（排除双端皆空页） |
| 主指标 `title_exact` | `norm_text(pred) == norm_text(gold)` 记 1，否则 0。**是精确匹配，不是相似度** |
| 漏提 / 崩溃 | pred 为 null → 0，入分母 |
| 误填 | gold 为空而 pred 非空 → 0，入分母，并计入误填统计 |
| 双端皆空 | 不进分母，计 `correct_empty` |

**诊断指标 `title_sim`**：pred 与 gold 规范化后的字符 3-gram 多重集 F1（定义同 §5 content 主指标）。仅作诊断不进主指标，用于区分两类失败：

- `title_exact = 0` 且 `title_sim` 高（经验阈值 >0.5）：多为"多带站点后缀 / 栏目名"型错误；
- `title_exact = 0` 且 `title_sim` 低：整体错位（抓到了导航、h1 信任危机等）。

---

## §4 authors

| 项 | 规定 |
|---|---|
| 单位 | 人名集合（无序、去重）。authors 是列表，可含机构署名 |
| 归一化 `norm_author` | NFKC + casefold → 剥角色括号（`张三（摄影）`→`张三`）→ 剥署名前缀（`记者`/`通讯员`/`作者`/`执笔`/`By` 等，可叠加剥离，如`记者通讯员张三`）→ 删除全部空白 |
| 主指标 `authors_f1` | 归一化后两集合的 P/R/F1：P = \|pred∩gold\|/\|pred\|，R = \|pred∩gold\|/\|gold\| |
| 双端皆空 | F1 不计算（记 None），不进分母，计 `correct_empty` |
| 漏提 | gold 非空、pred 空 → F1 = 0，入分母，计 `miss` |
| 误填 | gold 空、pred 非空 → F1 = 0，入分母，计 `false_fill` |

**必报派生指标**：

- `false_fill_rate` = 误填页数 / gold 无作者页数（gold 为空而 pred 非空的比例）；
- `miss_rate` = 漏提页数 / gold 有作者页数；
- `correct_empty` 计数。

**外部数据集注意**：若外部 gold 的作者字段覆盖率本身不全（"未标注"不等于"页面无作者"），`false_fill_rate` 会系统性偏高——必须在报告中如实声明该数据集局限，不得静默采信。

---

## §5 time

time 是多字段（publish / update 等分别维护、互不混淆）。每个时间字段独立评分。

### 5.1 主指标：按 gold 的精度比较

- 时间串格式 `YYYY-MM-DD hh:mm:ss`，未知分量以 `XX` 占位（见字段规范 v1.2）。
- **gold 中凡含 `X` 的分量不参与比较**；参与的分量（年/月/日/时/分/秒逐一）全部相等记 1，任一不等记 0。即：gold 到日就比到日，gold 到秒就比到秒。
- gold 尾部整体缺失等同于全 `XX`（gold `"2024-03-21"` 等价于 `"2024-03-21 XX:XX:XX"`）。
- pred 为 null（漏提/崩溃）→ 0，入分母；gold 空而 pred 非空 → 0 入分母并计误填；双端皆空 → correct_empty。

### 5.2 必报派生指标

- `time_confusion`：pred 的 publish 值等于该页 gold 的 **update**（或其他时间字段）值 → 单独标记计数。语义：抽错了字段 ≠ 解析错了串，归因时必须区分。
- **时区不进主指标**。`utc_offset` 单独三态统计：`match` / `missing`（pred 缺）/ `mismatch`（pred 有但与 gold 不等）。gold 本身无时区信息的页面记 `gold_absent`，不进这三态分母。
- 误填率、漏提率必报。

---

## §6 content（content_text）

### 6.1 主指标：规范化后字符 3-gram 多重集 F1

1. pred、gold 各自过 `norm_text`（§2）；
2. 取**字符 3-gram 多重集**（保留重复次数；长度 <3 的文本退化为全串单元素集合）；
3. 交集大小 = 各 gram 计数的 min 之和；P = 交集/|pred 多重集|，R = 交集/|gold 多重集|，F1 = 2PR/(P+R)。

**选型理由（冻结，不再讨论）**：

- **字符级而非词级**：中文免分词、英文免 tokenizer 差异，中英同规则（§1.7）；
- **n = 3**：乱序敏感度与错字宽容度的折中——n=1 对乱序完全不敏感，n≥5 对个别错字/标点差异过度惩罚；
- **多重集而非集合**：集合无法惩罚"同一段重复粘贴"（dup 场景 F1 会虚高为 1），多重集使其按 §8-S4 正确降为 0.5。

### 6.2 诊断指标（四个，必算必报，不进主指标）

| 指标 | 定义 | 读法 |
|---|---|---|
| `lcs_f1` | 规范化后字符级 LCS 长度（SequenceMatcher matching_blocks 之和）的 F1 = 2·LCS/(len_p+len_g) | 顺序敏感。`lcs_f1` 明显低于主指标 → 乱序；二者接近 → 失分来自漏段/混入而非乱序 |
| `dup_ratio` | pred 3-gram 多重集中重复出现的比例 = (总数−去重数)/总数 | 重复抓取。**注意：英文长文本天然有约 0.3~0.5 的重复 3-gram，该指标绝对值不可跨语言直接解读，只能同数据集内跨版本相对比** |
| `num_f1` | 数字串（正则 `\d+(\.\d+)?`）多重集 F1 | 关键数字保真度。显著低于主指标 → 正文在丢数字（表格/数据段落丢失的常见信号） |
| `para_ratio` | 段落数之比 min(pred_paras, gold_paras)/max(...)，段落按 1 个以上换行切分 | 段落结构保持度。双端皆 0 段时为 None |

### 6.3 空值处理

- gold content 为空且 pred 为空 → `correct_empty`，不进分母（适用于 video_page 等"正文本应为空"的页面）；
- gold 空 pred 非空 → 0 入分母（误填，常见于把导航当正文）；
- gold 非空 pred 空 → 0 入分母（漏提，常见于薄页召回失败）。

---

## §7 拒识（非文章页判别）

- 适用对象：`content_structure` 非 single 的页面、付费墙无可抽取页、`page_condition` 非 ok 页。
- **判定**：提取器对该页返回空记录（四字段全空 / 显式拒识标记）= 正确；返回了实质内容（把列表页、导航误抽成文章）= 错误。
- **指标**：拒识准确率 = 正确拒识页数 / 应拒识页数。误抽页必须列入 badcase 清单。
- 拒识页不进入 §3~§6 的字段评分分母（§1.5）；文章页也不进入拒识分母。两个分母互斥、合计 = 参评总页数。

---

## §8 手算例子（13 场景 / 46 断言）

以下例子是规范的**规范性内容**（normative），`score_v2.py` 必须以单元测试覆盖。全部断言已由 `eval/handcheck_draft.py` 实际运行通过（46/46）。

| # | 场景 | 输入（pred vs gold） | 期望结果 |
|---|---|---|---|
| S1 | 全对 | 各字段 pred = gold（作者 pred 带角色括号 `张三（摄影）`） | title_exact=1；title_sim=1.0；authors_f1=1.0；time=1.0；content F1=1.0；dup_ratio=0；lcs_f1=1.0；num_f1=1.0；para_ratio=1.0 |
| S2 | 漏半篇正文 | `"abcd"` vs `"abcdefgh"` | 交集 2，pred 2 克、gold 6 克 → P=1，R=1/3，**F1=0.5** |
| S3 | 混入导航 | `"abcdef导航栏"` vs `"abcdef"` | 交集 4，pred 7 克、gold 4 克 → P=4/7，R=1，**F1=8/11≈0.7273** |
| S4 | 重复一遍 | `"abcdabcd"` vs `"abcd"` | pred 6 克（abc×2、bcd×2、cda、dab），交集 2 → P=1/3，R=1，**F1=0.5**；**dup_ratio=(6−4)/6=1/3** |
| S5 | 乱序 | `"efghabcd"` vs `"abcdefgh"` | 交集 4，各 6 克 → **F1=2/3**；LCS=4 → **lcs_f1=0.5 < 2/3**（诊断正确指示乱序） |
| S6 | 作者误填 / 双端皆空 | pred `["张三"]` vs gold `[]`；pred `[]` vs gold `[]` | 前者 F1=0 且计 false_fill；后者 F1=None，**不进分母**，计 correct_empty |
| S7 | 作者前缀 | pred `["记者张三"]` vs gold `["张三"]` | norm_author 剥前缀后相等 → **F1=1.0**；未经作者归一化的原始串比较必然不等（反证该归一化必要） |
| S8 | 作者漏一 | pred `["张三"]` vs gold `["张三","李四"]` | P=1，R=1/2 → **F1=2/3** |
| S9 | 时间类型混淆 | gold publish=`2024-03-21 08:00:00`，update=`2024-03-25 09:00:00`；pred publish 填了 update 值 | 主指标 **0**；`time_confusion` 标记 = true；正确填写时 confusion = false |
| S10 | 时区缺失 | 时间值全对，pred utc_offset=null，gold=`+08:00` | 主指标 **1.0**（时区不进主指标）；时区统计记 `missing`；pred=`+08:00` 记 `match`；pred=`+00:00` 记 `mismatch` |
| S11 | 月精度 | gold=`XXXX-09`（年仅知占位、月已知）；pred1=`2024-09-15 10:00:00`，pred2=`2024-10-01` | 仅月分量参与：pred1 → **1.0**，pred2 → **0.0** |
| S12 | 崩溃计 0 | 提取器崩溃，pred=None | title=0、content=0、time=0，**全部入分母**；宏平均演示：1 页崩溃 + 1 页全对 → title 宏平均 = (0+1)/2 = **0.5** |
| S13 | 标题多后缀 | pred `美联储宣布加息_新浪财经` vs gold `美联储宣布加息` | title_exact=**0**；title_sim：gold 5 克、pred 10 克、交集 5 → **2/3≈0.6667**（>0.5，诊断判为"后缀型"而非"整体错位"） |

**分场景断言数**：S1×9、S2×3、S3×4、S4×4、S5×3、S6×3、S7×3、S8×1、S9×3、S10×4、S11×2、S12×4、S13×3，**合计 46 项**。

---

## §9 冻结条款

1. 本规范与 `score_v2.py` **绑定冻结**：两者任一修订，另一必须同步，且全部满足 §8 的 46 项断言。
2. 任何修订必须：① 在 `decision_log` 登记（编号、理由、影响面）；② **重跑全部对照实验**（所有数据集 × 所有提取器版本），新旧口径分数并列保留，不得用新口径覆盖旧结果制造"提升"。
3. 预留测试集纪律延续：测试集只在交付节点运行、只输出聚合分、不出逐页错误反馈；调优只依据开发集与外部开发子集。
4. 诊断指标（title_sim / lcs_f1 / dup_ratio / num_f1 / para_ratio / 时区三态 / time_confusion / false_fill_rate / miss_rate）与主指标同等级别"必算必报"，但**不得**用任何诊断指标替代主指标排名或宣称提升。
